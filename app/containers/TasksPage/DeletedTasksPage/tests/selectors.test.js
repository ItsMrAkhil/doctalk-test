// import { fromJS } from 'immutable';
// import { makeSelectDeletedTasksPageDomain } from '../selectors';

// const selector = makeSelectDeletedTasksPageDomain();

describe('makeSelectDeletedTasksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
