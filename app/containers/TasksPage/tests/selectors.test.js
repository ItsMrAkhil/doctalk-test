// import { fromJS } from 'immutable';
// import { makeSelectTasksPageDomain } from '../selectors';

// const selector = makeSelectTasksPageDomain();

describe('makeSelectTasksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
