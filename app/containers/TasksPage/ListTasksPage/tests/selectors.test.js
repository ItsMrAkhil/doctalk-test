// import { fromJS } from 'immutable';
// import { makeSelectListTasksPageDomain } from '../selectors';

// const selector = makeSelectListTasksPageDomain();

describe('makeSelectListTasksPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
